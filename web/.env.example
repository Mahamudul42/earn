# Base URL of the Django backend API. Exposed to the browser, so it must be
# prefixed with NEXT_PUBLIC_. In docker-compose this points at the backend
# service; for local dev it is http://localhost:8000.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
